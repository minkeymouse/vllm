# Common dependencies
-r common.txt

# 0.61.x caps numpy<2.3; kagglenemotron uses numpy 2.4.x — bump with smoke tests if speculative decode regresses.
numba == 0.65.0

# Dependencies for NVIDIA GPUs
# Kept in sync with kagglenemotron torch 2.11.0+cu128 (see root pyproject / uv.lock).
torch==2.11.0
torchaudio==2.11.0
# These must be updated alongside torch
torchvision==0.26.0 # PyTorch 2.11 companion; see https://github.com/pytorch/vision#installation
# FlashInfer should be updated together with the Dockerfile
flashinfer-python==0.6.7
flashinfer-cubin==0.6.7
# Cap nvidia-cudnn-frontend (transitive dep of flashinfer) due to
# breaking changes in 1.19.0
nvidia-cudnn-frontend>=1.13.0,<1.19.0

# QuACK and Cutlass DSL for FA4 (cute-DSL implementation)
nvidia-cutlass-dsl>=4.4.2
quack-kernels>=0.3.3
